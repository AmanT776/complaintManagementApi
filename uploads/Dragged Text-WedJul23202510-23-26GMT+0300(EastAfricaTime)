<<<<<<< Tutor
import React from "react"
import { TutorManagementProvider } from "./components/Context/TutorManagementContext"
import TutorManage from "./components/TutorManagement/Tutormanage"



const  App = ()=> {

  return (
    <>
      
      <TutorManagementProvider>
        <TutorManage />
      </TutorManagementProvider>
      
    </>
=======



<<<<<<< Tutor
import Payments from '@/pages/Payments';
import Dashboard from '@/Page/Tutor/Dashboard';
const Admin = ()=>{
=======


 const studentColumns = [
    { key: 'name', label: 'Name' },
    { key: 'email', label: 'Email' },
    {
      key: 'status',
      label: 'Status',
      render: (status) => (
        <span className={`px-2 py-1 rounded-full text-xs font-medium ${
          status === 'active'
            ? 'bg-green-100 text-green-800'
            : 'bg-gray-200 text-gray-800'
        }`}>
          {status}
        </span>
      )
    },
    {
      key: 'enrolledCourses',
      label: 'Enrolled Courses',
      render: (courses) => (
        <div className="flex flex-wrap gap-1">
          {courses.map((course, index) => (
            <span
              key={index}
              className="px-2 py-1 bg-blue-100 text-blue-800 text-xs rounded"
            >
              {course}
            </span>
          ))}
        </div>
      )
    },
    { key: 'grade', label: 'Grade' }
  ];

  const topTutors = mockTutors
    .filter(t => t.isVerified)
    .sort((a, b) => b.rating - a.rating)
    .slice(0, 5);

  return (
    <div className="space-y-6">
      {/* Header */}
      <h1 className="text-2xl font-bold dark:text-white text-gray-900">Dashboard Overview</h1>

      {/* Stats */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-6">
        <StatCard
          title="Total Students"
          value={mockStats.totalStudents.toLocaleString()}
          growth={mockStats.studentsGrowth}
          icon={<Users className="w-6 h-6" />}
          iconColor="bg-blue-100 text-blue-600"
        />
        <StatCard
          title="Total Tutors"
          value={mockStats.totalTutors}
          growth={mockStats.tutorsGrowth}
          icon={<GraduationCap className="w-6 h-6" />}
          iconColor="bg-green-100 text-green-600"
        />
        <StatCard
          title="Total Parents"
          value={mockStats.totalParents.toLocaleString()}
          growth={mockStats.parentsGrowth}
          icon={<UserCheck className="w-6 h-6" />}
          iconColor="bg-purple-100 text-purple-600"
        />
        <StatCard
          title="Active Sessions Today"
          value={mockStats.activeSessions}
          icon={<Calendar className="w-6 h-6" />}
          iconColor="bg-orange-100 text-orange-600"
        />
        <StatCard
          title="Monthly Revenue"
          value={`$${mockStats.monthlyRevenue.toLocaleString()}`}
          growth={mockStats.revenueGrowth}
          icon={<DollarSign className="w-6 h-6" />}
          iconColor="bg-green-100 text-green-600"
        />
      </div>

      {/* Main Content: Students and Tutors */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {/* Recent Students */}
        <div className="lg:col-span-2">
          <Card className='dark:bg-slate-900 dark:border-none'>
            <div className="space-y-4">
              <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <h2 className="text-lg font-semibold text-gray-900 dark:text-white">Recent Students</h2>
                <div className="flex gap-3">
                  <button
                    onClick={() => setShowStudentFilters(!showStudentFilters)}
                    className="inline-flex items-center px-3 py-2 border border-gray-300 rounded-lg text-sm text-gray-700 hover:bg-gray-50"
                  >
                    <Filter className="w-4 h-4 mr-2" />
                    Filters
                  </button>
                </div>
              </div>

              {/* Search and Filter */}
              {/* <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
                <div className="relative w-full sm:w-64">
                  <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400 w-4 h-4" />
                  <input
                    type="text"
                    placeholder="Search..."
                    value={studentSearchTerm}
                    onChange={(e) => setStudentSearchTerm(e.target.value)}
                    className="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500 text-sm"
                  />
                </div>
              </div> */}

              {/* Optional Filters */}
              {showStudentFilters && (
                <div className="pt-3 border-t border-gray-200 ">
                  <label className="block text-sm font-medium text-gray-700 mb-2">Status</label>
                  <select
                    value={studentStatusFilter}
                    onChange={(e) => setStudentStatusFilter(e.target.value)}
                    className="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-blue-500 focus:border-blue-500 text-sm"
                  >
                    <option value="all">All Statuses</option>
                    <option value="active">Active</option>
                    <option value="inactive">Inactive</option>
                  </select>
                </div>
              )}

              {/* Students Table */}
              <Table
                columns={studentColumns}
                data={filteredStudents.slice(0, 5)}
                showPagination={true}
                currentPage={1}
                totalPages={2}
                onPageChange={() => {}}
              />
            </div>
          </Card>
        </div>

        {/* Top-Rated Tutors */}
        <Card className='dark:bg-slate-900 dark:border-none'>
          <div className="space-y-4">
            <h2 className="text-lg dark:text-white  font-semibold text-gray-900">Top-Rated Tutors</h2>
            {topTutors.map((tutor, index) => (
              <div
                key={tutor.id}
                className="flex items-center gap-3 p-2 rounded-lg hover:bg-gray-50"
              >
                <div className="w-8 h-8 flex items-center justify-center bg-blue-600 text-white rounded-full text-sm font-bold">
                  {index + 1}
                </div>
                <div className="flex-1">
                  <p className="text-sm dark:text-white font-medium text-gray-900 truncate">{tutor.name}</p>
                  <p className="text-xs text-gray-500 truncate">{tutor.subjects.join(', ')}</p>
                </div>
                <div className="flex items-center text-sm font-medium text-gray-900">
                  <Star className="w-4 h-4 text-yellow-400 fill-current mr-1" />
                  {tutor.rating}
                </div>
              </div>
            ))}
          </div>
        </Card>
      </div>

      {/* Calendar */}
      <Card className='dark:bg-slate-900 dark:border-none'>
        <div className="flex items-center justify-between mb-4">
          <h2 className="text-lg font-semibold text-gray-900 dark:text-white">Calendar</h2>
          <span className="text-sm text-gray-500 dark:text-white">June 2025</span>
        </div>
        <div className="grid grid-cols-7 gap-2 text-center">
          {['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'].map((day) => (
            <div key={day} className="py-2 text-sm font-medium dark:text-white text-gray-500">
              {day}
            </div>
          ))}
          {Array.from({ length: 35 }, (_, i) => {
            const date = i - 4;
            const isCurrentMonth = date > 0 && date <= 30;
            const hasEvent = [10, 17, 25].includes(date);
            return (
              <div
                key={i}
                className={`py-2 text-sm rounded ${
                  isCurrentMonth
                    ? hasEvent
                      ? 'bg-blue-100  text-blue-900 font-medium'
                      : 'dark:text-white text-gray-900'
                    : 'text-gray-300'
                }`}
              >
                {isCurrentMonth ? date : ''}
              </div>
            );
          })}
        </div>
      </Card>
    </div>
  );
};

export default Dashboard;

